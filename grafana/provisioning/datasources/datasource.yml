apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    url: db:5432
    user: postgres
    secureJsonData:
      password: postgres
    jsonData:
      database: turbina_db
      sslmode: "disable"
    editable: true